{
  "name": "simple-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:bike-store": "npm --prefix bike-store run build",
    "build:car-store": "npm --prefix car-store run build",
    "all-the-things": "cd bike-store && npm i && npm run build && cd ../car-store && npm i && npm run build"
  }
}
