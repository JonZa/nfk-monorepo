{
  "name": "nfk-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "react-next-build": "npm run build",
    "vue-nuxt-build": "npm run build",
    "all-the-things": "cd react-next && npm i && npm run build && cd ../vue-nuxt && npm i && npm run build"
  }
}
